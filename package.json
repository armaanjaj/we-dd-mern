{
  "name": "server",
  "version": "2.0.0",
  "description": "This is the we-dd.ca backend created using NodeJS and ExpressJS.",
  "main": "server/App.js",
  "scripts": {
    "start": "cd server && npx nodemon App.js",
    "build": "cd client && npm install && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "18.12.1"
  },
  "dependencies": {
    "body-parser": "^1.20.0",
    "cookie-parser": "^1.4.6",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.8",
    "express": "^4.18.1",
    "express-session": "^1.17.3",
    "nodemailer": "^6.8.0",
    "nodemailer-express-handlebars": "^6.0.0",
    "nodemon": "^2.0.19"
  }
}
