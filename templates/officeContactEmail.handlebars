<html>
    <head>
        <meta charset="utf-8" />
        <meta http-equiv="x-ua-compatible" content="ie=edge" />
        <title>New contact request</title>
    </head>
    <body>
        <div>
            <table>
                <tr>
                    <th>Name:</th>
                    <td>{{name}}</td>
                </tr>
                <tr>
                    <th>Email:</th>
                    <td>{{email}}</td>
                </tr>
            </table>
            <h3>{{message}}</h3>
        </div>
    </body>
</html>